package recursion;

public class RecursionFindMax {

    public static int findMax(int[] arr) {
        if (arr == null || arr.length == 0) return -1;
        int max = arr[0];

//        if (max < arr[1]) {
//
//        }
        return 0;
    }

}
